# in javascript
# Create metadata for the bookworm.

const fs = require('fs');

const readFile = () => {};

const determineIndustry = () => {};

const parse = (fileString) => {};

# {
   "searchstring" : companyName,
                 "filename" : filename,
                 "year" : year,
                 "industry" : subjectArray,
		 "ticker": ticker, ??
                }
